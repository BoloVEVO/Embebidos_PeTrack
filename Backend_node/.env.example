# Copiar como .env (NO se versiona). Backend del sistema de detección de mascotas.
PORT=3000

# Ruta al service account de Firebase (P5). Dejar vacío = modo dev sin Firebase
# (store local en JSON) para no bloquear el desarrollo.
GOOGLE_APPLICATION_CREDENTIALS=./secrets/serviceAccountKey.json
FIREBASE_PROJECT_ID=
